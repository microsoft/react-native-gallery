import { StyleSheet } from "react-native";
// Design Specs: https://aka.ms/subs-comps

export const styles = StyleSheet.create({
  coinWrapper: {
    alignItems: "center",
    flexDirection: "row"
  },
  coinCircle: {
    alignItems: "center",
    borderRadius: 1000,
    justifyContent: "center",
  },
  coinText: {
    color: "#FFF"
  }
});
